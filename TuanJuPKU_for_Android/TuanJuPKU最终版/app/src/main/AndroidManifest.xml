<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.elrid.tuanjupku" >
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:name=".app.MyApplication">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >

        </activity>

        <activity
            android:name=".CoverFlowActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".GetMainActivity"
            android:label="@string/app_name" >

        </activity>

        <activity
            android:name=".WebActivity"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".expandablelistitems.ExpandableListItemActivity"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".MyListActivity"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".BaseActivity"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".googlecards.GoogleCardsActivity"
            android:label="@string/app_name" >
        </activity>

    </application>

</manifest>
